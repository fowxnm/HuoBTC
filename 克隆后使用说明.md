# 克隆后直接使用

克隆本仓库后，按以下步骤即可在本地或服务器直接运行。

## 前置条件

- **Git**：用于克隆仓库  
- **Bun**：运行时与包管理（[安装](https://bun.sh)）  
- **PostgreSQL**：数据库（需先创建数据库 `btc_exchange`）  
- **Redis**：缓存与队列  

以上安装完成后，按下面步骤配置即可使用。

## 一、克隆

```bash
git clone https://github.com/fowxnm/BTC.git
cd BTC
```

## 二、一键配置（Windows）

双击运行 **`克隆后一键配置.bat`**，会自动：

- 在 `backend` 和 `frontend` 下从 `.env.example` 复制出 `.env`
- 在 `backend` 和 `frontend` 下执行 `bun install` 安装依赖

## 三、手动配置（与一键配置二选一）

若不用脚本，可手动执行：

```bash
# 后端
cd backend
copy .env.example .env   # Windows
# cp .env.example .env   # Linux/Mac
bun install
cd ..

# 前端
cd frontend
copy .env.example .env   # Windows
# cp .env.example .env   # Linux/Mac
bun install
cd ..
```

## 四、必填环境变量

编辑 **`backend/.env`**，至少填写：

- **DATABASE_URL**：PostgreSQL 连接串，例如  
  `postgres://postgres:你的密码@localhost:5432/btc_exchange`
- **REDIS_URL**：Redis 连接，例如  
  `redis://localhost:6379`

首次使用需先创建数据库：

```sql
CREATE DATABASE btc_exchange;
```

后端启动时会自动建表；若需预置管理员，在 `backend` 目录执行：

```bash
bun run src/scripts/seed-admins.ts
```

## 五、启动

### Windows 本地

双击 **`start-windows.bat`**，会同时启动后端（8000）和前端（3000）。

### 或分别启动

```bash
# 终端 1 - 后端
cd backend
bun run dev

# 终端 2 - 前端
cd frontend
bun run dev
```

- 前端：http://localhost:3000  
- 后端 API：http://localhost:8000  
- 管理后台登录：http://localhost:3000/admin/login  

## 六、服务器部署

1. 在服务器上克隆本仓库，按上面步骤执行「一键配置」或「手动配置」。
2. 填写 `backend/.env` 中的 DATABASE_URL、REDIS_URL 等（指向服务器上的 PostgreSQL、Redis）。
3. 前端构建：`cd frontend && bun run build`，将 `dist/` 用 Nginx 等托管，API 代理到后端 8000 端口。
4. 后端用 pm2 或 systemd 守护：`bun run dev` 或 `bun run start`（如有）。

完成以上步骤后，克隆即可直接使用。
