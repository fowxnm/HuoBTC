# 后续操作（必读）

## 1. 连接检查结果

已确认 **backend/.env** 中：

- **DATABASE_URL** = `postgres://postgres:postgres123@localhost:5432/btc_exchange`（格式正确）
- **REDIS_URL** = `redis://localhost:6379`（格式正确）

若你安装 PostgreSQL 时设置的密码**不是** `postgres123`，请把 `.env` 里的 `DATABASE_URL` 改成你的实际密码，例如：

```env
DATABASE_URL=postgres://postgres:你的实际密码@localhost:5432/btc_exchange
```

---

## 2. 初始化数据库（db:push 未成功时）

执行 `db:push` 时出现 **「password authentication failed for user postgres」**，说明当前 `.env` 里的密码和本机 PostgreSQL 不一致。

请按下面步骤做一次：

1. 打开 **backend/.env**，把 **DATABASE_URL** 里的密码改成你本机 PostgreSQL 的 postgres 用户密码。
2. 在项目根目录打开终端，执行：

```bat
cd backend
bun run db:push
```

出现 “Push completed” 或类似成功提示即表示表结构已同步到 PostgreSQL（用户、订单、K 线等表）。

---

## 3. 后端与前端已启动

已为你打开两个 CMD 窗口：

- **BTC Backend**：后端（端口 8000），启动时会立刻请求一次币安 + FMP + Exchangerate，把实时价格写入 Redis。
- **BTC Frontend**：前端（端口 3000）。

若后端窗口里出现数据库连接错误，请先按上面第 2 步修正密码并成功执行 `bun run db:push`，然后关闭后端窗口，再在 **backend** 目录重新执行：

```bat
bun run dev
```

---

## 4. 本地访问地址

- **前端页面**：在浏览器打开  
  **http://localhost:3000**  
- **后端 API**：  
  **http://localhost:8000**  
- **健康检查**：  
  **http://localhost:8000/health**  
- **API 文档**：  
  **http://localhost:8000/docs**

前端通过 Vite 代理把 `/api` 请求转到后端 8000，无需改前端配置。
