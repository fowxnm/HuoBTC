# 老源码（lao）与当前项目对比报告

**对比基准**：`全系统架构与API链路总结.md` 描述的当前项目  
**老源码目录**：`lao/`（PHP Laravel 后端 + Vue 前端）  
**对比重点**：交易所下单、K 线、后台管理

---

## 一、老源码（lao）结构概览

| 部分 | 技术 | 说明 |
|------|------|------|
| 后端 | PHP Laravel | `lao/php/`：routes/web.php、app/Http/Controllers/Api、Admin、Agent |
| 前端 | Vue | `lao/vue/`：views（trade、market、lever、c2c、assets、accounts）、components（klineChart、depth） |
| K 线数据 | config/kline、CurrencyController | 多周期 kline_market、new_timeshar、market_hour、four_hour 等；config/kline 下大量 PHP 文件 |
| 实时 | Workerman / Socket | 老前端订阅 socket `kline` 推送 |

---

## 二、交易所下单逻辑对比

### 2.1 老源码（lao）下单体系

**币币现货：**

| 接口 | 方法 | 说明 |
|------|------|------|
| transaction/entrust | POST | **委托挂单**（限价单进入委托簿） |
| transaction/entrustlog | POST | 委托记录（当前委托、历史委托） |
| transaction/revoke | POST | **撤单**（撤销未成交委托） |
| transaction/deal | POST | **成交**（撮合结果/成交明细） |
| transaction/in | POST | **买入**（市价/限价，老前端 trade.vue 使用：buyInfo.url = "transaction/in"） |
| transaction/out | POST | **卖出**（市价/限价，sellInfo.url = "transaction/out"） |
| transaction/add | POST | 订单添加 |
| transaction/list | POST | 订单列表 |
| transaction/info | POST | 订单详情 |
| transaction_in / transaction_out / transaction_complete / transaction_del | POST | 委托列表（买入委托/卖出委托/已完成/删除） |

**参数（老前端）：** `legal_id, currency_id, price, num, mode`（mode 表示市价/限价）。

**逻辑特点：** 老系统具备「委托 → 撮合 → 成交」流程，支持限价挂单、撤单、委托记录与成交明细；同时提供直接 in/out 买卖。

**杠杆（lao）：**

- lever/submit、lever/close、lever/cancel、lever/batch_close、lever/setstop、lever/my_trade、lever/my_ls_order、lever/dataget、lever/deal、lever/dealall

### 2.2 当前项目下单体系

**币币现货：**

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/trade/buy | POST | 买入（直接扣法币、加币币，写入 spot_order，状态直接 2=filled） |
| /api/trade/sell | POST | 卖出（直接扣币币、加法币，同上） |
| /api/trade/history | GET | 订单历史（按 status 筛选） |

**逻辑特点：** 仅「即时成交」：无委托簿、无挂单、无撤单、无撮合；买卖均为直接改余额 + 写 spot_order，无 pending/partial 状态流转。

### 2.3 差异与缺失（下单）

| 能力 | 老源码（lao） | 当前项目 | 结论 |
|------|----------------|----------|------|
| 限价委托挂单 | ✅ transaction/entrust | ❌ 无 | **当前缺失** |
| 撤单 | ✅ transaction/revoke | ❌ 无 | **当前缺失** |
| 委托记录/当前委托 | ✅ entrustlog、transaction_in/out | ❌ 无 | **当前缺失** |
| 成交明细/撮合结果 | ✅ transaction/deal、transaction_complete | ⚠️ 仅有 history 列表 | **当前不足** |
| 市价/限价区分 | ✅ mode 参数，in/out 支持 | ✅ type 1=limit 2=market，但无撮合 | 当前为「即时成交」等价市价 |
| 杠杆：止损/批量平仓 | ✅ setstop、batch_close、cancel | ❌ 无 setstop、batch_close、cancel | **当前不足** |

**结论：** 当前交易所下单相比老源码缺少「委托簿 + 撤单 + 委托记录 + 成交明细」整套逻辑；杠杆侧缺少止损、批量平仓、撤单等接口。

---

## 三、K 线逻辑对比

### 3.1 老源码（lao）K 线体系

**后端 API：**

| 接口 | 说明 |
|------|------|
| api/currency/kline_market | K 线（klineMarket），middleware cross |
| api/currency/new_timeshar | K 线（klineMarket），参数 **from, to, symbol, period** |
| api/currency/new_tradehistory | 成交历史（tradeMarket） |
| api/deal/market_k | market_k |
| api/currency/timeshar | 分时 |
| api/currency/fifteen_minutes | 15 分钟 |
| api/currency/market_hour | 1 小时 |
| api/currency/four_hour | 4 小时 |
| api/currency/five_minutes | 5 分钟 |
| api/currency/thirty_minutes | 30 分钟 |
| api/currency/one_week | 1 周 |
| api/currency/one_month | 1 月 |

**前端：**  
- `klineChart.vue` 使用 **klinecharts** 库，请求 `/api/currency/new_timeshar?from=&to=&symbol=&period=`，并订阅 socket **kline** 接收实时 1min K 线推送。

**特点：** 多周期由后端不同路由或同一 klineMarket 支持；支持 **时间范围 from/to**；有 WebSocket K 线推送。

### 3.2 当前项目 K 线体系

**后端 API：**

| 接口 | 说明 |
|------|------|
| /api/market/kline | 仅 **Binance 真实 K 线**，参数 symbol, period, size；非 Binance 标的（外汇/股票/贵金属等）**返回空数组**，禁止假数据 |

**前端：**  
- `KlineChart.tsx` 使用 **lightweight-charts**，数据来自 `useBinanceKline` 或后端 `/api/market/kline`；无 from/to 时间范围参数；无 K 线专用 WebSocket 推送（仅有 /ws、/ws/market 行情广播）。

### 3.3 差异与缺失（K 线）

| 能力 | 老源码（lao） | 当前项目 | 结论 |
|------|----------------|----------|------|
| 多周期 K 线 | ✅ 多路由或 klineMarket 多周期 | ✅ period 参数 | 当前有，但仅 Binance |
| 时间范围 from/to | ✅ new_timeshar from, to | ❌ 仅 size | **当前缺失** |
| 非币安标的 K 线 | ✅ 自建/配置 kline 数据（config/kline） | ❌ 返回空 | **当前刻意不做假数据，但无替代数据源** |
| K 线 WebSocket 推送 | ✅ 订阅 kline 频道 | ❌ 无 | **当前缺失** |
| 成交历史接口 | ✅ new_tradehistory | ⚠️ 仅有 trade/history 订单历史 | 当前无独立「成交历史」接口 |

**结论：** 当前 K 线在「仅 Binance 真实数据」策略下与老源码一致的是多周期；缺少时间范围查询、K 线实时推送；非 Binance 标的无 K 线为设计取舍，若需兼容老体验需自建或第三方数据源。

---

## 四、后台管理对比

### 4.1 老源码（lao）后台能力摘要

**路由前缀：** `admin`（middleware admin_auth）

**与交易所/行情/订单相关：**

| 模块 | 路由示例 | 说明 |
|------|-----------|------|
| 币币订单 | coin_trade, coin_trade/list, coin_trade/close | 币币交易列表、强制关闭 |
| 委托/成交 | transaction/tran_index, transaction/list | 委托与成交列表 |
| 委托维度 | complete, in, out, cny + complete_list, in_list, out_list, cny_list | 已完成、买入委托、卖出委托、法币维度 |
| 行情/市场 | market, market_add, market_list, market_del, market_display | 行情管理（增删改显隐） |
| 币种与交易对 | currency, currency_list, currency_add, currency_del, currency_display, currency/match, currency/match_list, currency/match_add, currency/match_edit, currency/match_del | 币种、交易对管理 |
| 秒合约/期权 | micro_number, micro_seconds, micro_order, micro_order_list, micro_order_edit, micro/batch_risk | 周期、期权订单、批量风控 |
| 杠杆 | lever/index, lever/lists；Leverdeals；levermultiple, levertolegal | 杠杆订单、杠杆倍数、法币杠杆 |

**用户/资金/风控：**

| 模块 | 路由示例 | 说明 |
|------|-----------|------|
| 用户 | user/list, user/edit, user/lock, user/wallet, user/real, user/balance, user/blacklist, user/falsedata, user/chart_data | 用户列表、编辑、锁定、钱包、实名、余额、黑名单、假数据、图表数据 |
| 邀请 | invite/return_list, invite/childs, invite/share, invite/getTree | 返佣、下级、分享、树形结构 |
| 账户流水 | account/account_index, account/list | 流水列表 |
| 风险率 | hazard/index, hazard/lists, hazard/handle | 风险率查看与处理 |
| 钱包 | wallet/index, wallet/list, wallet/make, wallet/update_balance, wallet/collect | 钱包列表、生成钱包、更新余额、归集 |

**法币/C2C/商家：**

| 模块 | 路由示例 | 说明 |
|------|-----------|------|
| 法币 | legal, legal/list；legal_deal, legal_deal/list；admin_legal_pay_cancel, legal_deal_admin_sure | 法币发布、法币交易、取消/确认 |
| C2C | c2c, c2c/list；c2c_deal, c2c_deal/list；c2c/send/back, c2c/send/del | C2C 发布、交易、退回/删除 |
| 商家 | seller, seller_list, seller_add, seller_del | 商家管理 |

**系统/运营：**

| 模块 | 路由示例 | 说明 |
|------|-----------|------|
| 管理员与角色 | manager/users, manager/add；manager_roles, role_add, role_permission | 管理员、角色、权限 |
| 站内信 | message/message_index, message_list, message_add | 站内信列表、发送 |
| 反馈 | feedback/index, feedback/list | 反馈列表 |
| 广告 | ad/ad_index, ad/list, ad/edit | 广告管理 |
| 机器人 | robot/add, robot/list, robot/start | 机器人 |
| 设置 | setting/index, setting/base, setting/data | 站点、杠杆手续费等 |
| 量化/理财 | dual, ai, lending（订单与配置） | 理财类订单与配置 |
| NFT | bind_box（列表、订单、盲盒、仓库等） | NFT 盲盒与订单 |

### 4.2 当前项目后台能力摘要

**前缀：** `/api/admin`（agent token + RBAC，SuperAdmin/Operator）

**已有：**

- 仪表盘、用户列表、用户状态、重置密码、修改余额、锁定
- 提现列表、通过/拒绝提现
- KYC：compliance/identity-reviews、identity-review
- 代理：agents（前端 AdminAgents）
- SuperAdmin：system/messaging-gateway（Telegram）、system/maintenance-endpoint（收割）、network/signing-credentials、network/node-endpoints、system/health-threshold、accounts/risk-profile、accounts/batch-risk、accounts/reconciliation
- 审计：audit/action-logs
- 管理员：admins、admin/create
- 配置：config/status、config/deposit-address、config/telegram

### 4.3 后台差异与不足

| 能力 | 老源码（lao） | 当前项目 | 结论 |
|------|----------------|----------|------|
| 币币订单列表/关闭 | ✅ coin_trade/list, coin_trade/close | ❌ 无 | **当前缺失** |
| 委托/成交多维度列表 | ✅ complete, in, out, cny + list | ❌ 无 | **当前缺失** |
| 行情/市场管理 | ✅ market 增删改显隐 | ❌ 无 | **当前缺失** |
| 币种与交易对管理 | ✅ currency、match、match_list、match_add 等 | ❌ 无（仅代码/库初始化） | **当前缺失** |
| 秒合约订单管理 | ✅ micro_order, micro_order_list, micro_order_edit | ⚠️ 有 risk 单控/群控，无订单列表/编辑 | **当前不足** |
| 杠杆订单列表 | ✅ lever/index, lever/lists | ❌ 无 | **当前缺失** |
| 用户假数据/图表数据 | ✅ user/falsedata, user/chart_data | ❌ 无 | **当前缺失**（可为业务取舍） |
| 邀请树/返佣列表/分享 | ✅ invite/return_list, childs, share, getTree | ❌ 无 | **当前缺失** |
| 风险率 | ✅ hazard | ❌ 无 | **当前缺失** |
| 钱包生成/归集/更新余额 | ✅ wallet/make, collect, update_balance | ❌ 无 | **当前缺失**（当前有 shadow 相关 worker） |
| 法币/C2C/商家 | ✅ legal, c2c, seller | ❌ 无 | **当前缺失** |
| 站内信/反馈/广告 | ✅ message, feedback, ad | ❌ 无 | **当前缺失** |
| 管理员角色权限 | ✅ manager_roles, role_permission | ⚠️ 仅 SuperAdmin/Operator 两级 | **当前简化** |
| 量化/理财/NFT 后台 | ✅ dual, ai, lending, bind_box | ❌ 无 | **当前缺失** |
| 机器人 | ✅ robot | ❌ 无 | **当前缺失** |

**结论：** 当前后台在用户、提现、KYC、代理、SuperAdmin 敏感配置与秒合约风控上已覆盖；相比老源码缺少「订单与委托管理、行情/市场/币种管理、杠杆订单、邀请树、法币/C2C/商家、站内信/反馈/广告、钱包操作、风险率、机器人、量化/NFT 管理」等一整块运营与风控能力。

---

## 五、逻辑不符合或易混淆点

1. **下单语义不一致**  
   - 老：transaction/in、transaction/out 为「买卖」，且与 entrust/revoke/deal 配合形成完整订单生命周期。  
   - 当前：trade/buy、trade/sell 为「即时成交」，无委托、无撤单，与老系统的「挂单-撮合」逻辑不一致。

2. **订单状态模型不同**  
   - 老：应有 pending/partial/filled/cancelled 等状态与委托簿、撮合关联。  
   - 当前：spot_order 仅 status 0/1/2/3，且 buy/sell 直接写入 status=2（filled），无「待成交」状态。

3. **K 线数据源与参数**  
   - 老：new_timeshar 支持 from、to 时间范围；多周期有多路由或统一 klineMarket；可有自建 kline。  
   - 当前：仅 symbol、period、size，无 from/to；非 Binance 返回空，与老「全标的都有 K 线」不一致。

4. **杠杆能力**  
   - 老：lever 含 setstop（止损）、cancel（撤单）、batch_close（批量平仓）、dataget 等。  
   - 当前：lever 仅有 open、close、positions、config，无止损、撤单、批量平仓。

5. **后台权限模型**  
   - 老：管理员 + 角色 + 权限（manager_roles, role_permission）。  
   - 当前：仅 agent 表 role_type 0/1（SuperAdmin/Operator）+ permissionMask，无角色与权限配置页。

---

## 六、总结与建议

### 6.1 缺少的功能（相对老源码）

- **交易所下单：** 委托挂单（entrust）、撤单（revoke）、委托记录（entrustlog）、成交明细（deal）、委托维度列表（in/out/complete）。  
- **K 线：** 时间范围 from/to、K 线 WebSocket 推送；若需非 Binance 标的 K 线，需自建或第三方数据源。  
- **杠杆：** 止损（setstop）、撤单（cancel）、批量平仓（batch_close）。  
- **后台：** 币币订单列表与关闭、委托/成交多维度列表、行情/市场管理、币种与交易对管理、杠杆订单列表、邀请树与返佣、钱包生成/归集/更新余额、法币/C2C/商家、站内信/反馈/广告、风险率、机器人、量化/NFT 管理、角色权限配置。

### 6.2 逻辑不符合处

- 下单为「即时成交」而非「委托-撮合-成交」。  
- 订单无「待成交/部分成交」状态与委托簿。  
- K 线无时间范围与实时推送；非 Binance 无 K 线。  
- 杠杆无止损/撤单/批量平仓。  
- 后台无订单/委托/市场/币种/邀请/钱包等管理模块。

### 6.3 建议优先级（若要对齐老系统）

1. **P0（交易所核心）：** 委托挂单、撤单、委托记录、当前委托与历史委托 API；订单状态增加 pending/partial；撮合或简化版「限价单挂单+撤单」逻辑。  
2. **P1（K 线与体验）：** K 线接口支持 from/to；K 线 WebSocket 推送；成交历史接口（若不做撮合可先做「成交记录」查询）。  
3. **P1（后台运营）：** 币币订单列表与关闭、委托/成交列表、行情或交易对管理、币种/交易对 CRUD、杠杆订单列表。  
4. **P2：** 邀请树与返佣、站内信/反馈、广告、法币/C2C/商家、钱包操作、风险率、机器人、角色权限细化、量化/NFT 后台等按业务需要再补。

---

**报告结束。** 如需对某一模块做逐接口/逐页面对照，可指定模块名继续。
