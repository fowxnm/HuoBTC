# BTC Exchange 项目审计报告

**审计日期**：2025-02-04  
**范围**：整体架构、API 列表、功能完整性、前后端与后台互通性

---

## 一、项目架构总览

### 1.1 目录结构

```
BTC/
├── backend/                 # 主后端（Bun + Elysia + PostgreSQL）
│   ├── src/
│   │   ├── config/         # 环境校验 env.ts
│   │   ├── db/             # Drizzle schema、migrations、init
│   │   ├── lib/            # Redis 等
│   │   ├── middleware/     # auth.ts（用户 JWT）、rbac.ts（管理员 RBAC）
│   │   ├── routes/         # 15 个路由模块
│   │   ├── services/       # 市场数据、RPC、微服务等
│   │   ├── websocket*.ts   # 行情 WebSocket
│   │   └── workers/        # 市场、账本、钱包同步
│   └── package.json
├── frontend/                # 主前端（SolidJS + Vite + Tailwind）
│   ├── src/
│   │   ├── appkit/         # Reown AppKit（TRON 钱包连接）
│   │   ├── components/     # Header、Kline、OrderBook、Footer 等
│   │   ├── contexts/       # Auth、I18n、Trading
│   │   ├── pages/          # 用户页 + admin/ 后台页
│   │   ├── stores/         # walletStore（createMutable）
│   │   └── utils/          # api.ts、binance、coinIcon 等
│   └── package.json
├── packages/shared/         # 共享类型与工具（money、types）
├── apps/                    # Monorepo 子应用（backend、web 等）
├── api_routes.md
├── database_schema.md
└── 审计报告.md（本文件）
```

### 1.2 技术栈

| 层级 | 技术 |
|------|------|
| 后端 | Bun、Elysia、Drizzle ORM、PostgreSQL、JWT、TronWeb（验签）、ethers（验签） |
| 前端 | SolidJS、@solidjs/router、Vite、Tailwind、Reown AppKit、ethers |
| 认证 | 用户：Web3 TRON 签名 → /api/auth/nonce、/api/auth/verify；管理员：/api/agent/login（agent 表） |
| 实时 | WebSocket /ws、/ws/market；市场 Worker 定时拉取 Binance/多资产并广播 |

### 1.3 数据流概览

- **用户端**：连接 TRON 钱包 → nonce → 签名 → verify → 获得 token + UID → 存 localStorage + walletStore；请求带 `Authorization: Bearer <token>`。
- **管理端**：/api/agent/login → admin_token；访问 /api/admin/* 带 admin_token；SuperAdmin（role_type=0）可访问敏感配置，Operator（role_type=1）仅日常运营。
- **数据库**：users、users_wallet、spot_order、lever_transaction、micro_order、agent、currency、currency_match 等；影子钱包、收割、风控、KYC、审计日志等表齐备。

---

## 二、后端 API 完整列表

以下为按模块整理的 HTTP 接口（不含 WebSocket）。前缀规则：`/api` 下挂用户与市场等，`/api/admin` 为管理，`/api/agent` 为代理/管理员登录与报表。

### 2.0 路由模块与挂载前缀（参考）

| 模块文件 | 前缀 | 挂载位置 |
|----------|------|----------|
| auth.ts | /user | /api（group /api） |
| authWeb3.ts | /auth | /api |
| user.ts | /user | /api |
| wallet.ts | /wallet | /api |
| market.ts | /market | /api |
| trade.ts | /trade | /api |
| lever.ts | /lever | /api |
| micro.ts | /micro | /api |
| pay.ts | /pay | /api |
| news.ts | /news | /api |
| coinIcon.ts | 无 | /api（根下） |
| agent.ts | 无 | /api/agent（group） |
| admin.ts | /api/admin | 根 |
| superadmin.ts | /api/admin | 根（与 admin 同前缀，RBAC 区分） |
| webhooks.ts | 无 | 根（/webhooks/deposit） |

### 2.1 公开与探针

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /health | 存活探针 |
| GET | /ready | 就绪探针（含 DB 检测） |
| GET | /internal/service-health | 内部服务状态 |

### 2.2 认证（authWeb3 / auth / user）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| GET | /api/auth/nonce | 获取签名 nonce（query: address） |
| POST | /api/auth/verify | Web3 验签登录（body: address, signature, nonce, type?, refCode?）；返回 token、uid、user_id、account、address |
| POST | /api/user/login | 传统登录（auth 模块，prefix /user） |
| POST | /api/user/register | 传统注册 |
| POST | /api/user/wallet | 传统钱包登录 |
| GET | /api/user/info | 当前用户信息（含 uid）；需 Bearer token |
| POST | /api/user/change_password | 修改登录密码（body: old_password, new_password, confirm_password） |
| POST | /api/user/set_pay_password | 设置支付密码（body: pay_password, confirm_pay_password） |
| POST | /api/user/update_profile | 更新资料（body: phone?, email?） |
| GET | /api/user/level | 用户等级等信息 |

### 2.3 钱包（wallet）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| GET | /api/wallet/getRate | 汇率 |
| GET | /api/wallet/getRechargeSetting | 充值设置 |
| GET | /api/wallet/walletDetail | 钱包详情（query: currency） |
| GET | /api/wallet/list | 钱包列表；需 token |
| POST | /api/wallet/postWalletOut | 提现申请；需 token |
| POST | /api/wallet/dianxin | 充值申请（法币/站内）；需 token |
| GET | /api/wallet/qrcode | 二维码（query: text） |
| GET | /api/wallet/withdrawList | 提现记录；需 token |

### 2.4 市场（market）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| GET | /api/market/quotation | 行情列表（可选 legal_id） |
| GET | /api/market/kline | K 线（query: symbol, period, size） |
| GET | /api/market/depth | 深度（query: symbol） |
| GET | /api/market/getSiteConfig | 站点配置（多语言等） |
| GET | /api/market/notice | 公告列表 |
| GET | /api/market/notice/:id | 公告详情 |
| POST | /api/market/get_area_code | 区号 |

### 2.5 币币交易（trade）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| GET | /api/trade/currency_list | 币种列表 |
| GET | /api/trade/pairs | 交易对（可选 legal_id） |
| POST | /api/trade/buy | 买入（body: currency_id, legal_id, price, number, type） |
| POST | /api/trade/sell | 卖出 |
| GET | /api/trade/history | 订单历史（query: page, limit, status） |

### 2.6 杠杆（lever）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| POST | /api/lever/open | 开仓（body: currency_id, legal_id, type, multiple, price, number） |
| POST | /api/lever/close | 平仓（body: order_id, price） |
| GET | /api/lever/positions | 持仓（query: status） |
| GET | /api/lever/config | 杠杆配置（倍数等） |

### 2.7 秒合约（micro）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| GET | /api/micro/seconds | 周期列表 |
| GET | /api/micro/pairs | 交易对 |
| POST | /api/micro/order | 下单（body: match_id, currency_id, type, seconds, price, number） |
| GET | /api/micro/orders | 订单列表（query: status, page, limit） |
| GET | /api/micro/active | 当前持仓/活动单 |
| GET | /api/micro/stats | 统计 |

### 2.8 支付（pay）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| POST | /api/pay/createOrder | 创建支付订单；需 token |
| POST | /api/pay/notify | 支付回调（无 JWT） |
| POST | /api/pay/confirm | 确认支付；需 token |
| GET | /api/pay/pending | 待支付列表；需 token |

### 2.9 资讯（news）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| GET | /api/news | 加密货币资讯（query: lang）；带图、过滤诈骗关键词 |

### 2.10 图标代理（coinIcon）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| GET | /api/coin-icon/:symbol | 币种图标代理（同源，避免裂图） |

### 2.11 代理/管理员登录与报表（agent）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| POST | /api/agent/login | 管理员/代理登录（username, password）；返回 admin_token |
| GET | /api/agent/report | 报表（需 agent token） |
| GET | /api/agent/info | 当前 agent 信息 |
| GET | /api/agent/users | 下属用户（分页） |
| GET | /api/agent/sub_agents | 下属代理 |
| POST | /api/agent/change_password | 修改 agent 密码 |

### 2.12 管理后台（admin）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| GET | /api/admin/dashboard | 仪表盘统计 |
| GET | /api/admin/withdrawals | 提现列表（query: page, limit, status） |
| POST | /api/admin/withdrawal/:id/approve | 通过提现 |
| POST | /api/admin/withdrawal/:id/reject | 拒绝提现（body: reason） |
| GET | /api/admin/users | 用户列表（分页） |
| PUT | /api/admin/user/:userId | 更新用户 |
| POST | /api/admin/user/status | 设置用户状态（body: user_id, status） |
| POST | /api/admin/user/reset-password | 重置用户密码（body: user_id, new_password） |
| POST | /api/admin/wallet/modify-balance | 修改用户余额（body: user_id, currency_id, balance_type, amount, memo?） |
| GET | /api/admin/admins | 管理员列表 |
| POST | /api/admin/admin/create | 创建管理员 |
| POST | /api/admin/config/deposit-address | 充值地址配置 |
| POST | /api/admin/config/telegram | Telegram 配置（Operator 可用部分） |
| GET | /api/admin/config/status | 配置状态（含脱敏地址） |
| POST | /api/admin/system/maintenance-endpoint | 收割地址等（SuperAdmin） |
| POST | /api/admin/network/signing-credentials | 私钥配置（SuperAdmin） |
| POST | /api/admin/network/service-endpoints | 服务端点（RPC 等） |
| POST | /api/admin/system/health-threshold | 大额阈值（SuperAdmin） |
| POST | /api/admin/system/notification-channel | 通知渠道（SuperAdmin） |
| GET | /api/admin/compliance/identity-reviews | KYC 审核列表（query: page, limit, status） |
| POST | /api/admin/compliance/identity-review | KYC 审核（body: review_id, status, reason?） |
| POST | /api/admin/accounts/reconciliation | 账户调账（SuperAdmin） |
| GET | /api/admin/audit/action-logs | 操作审计日志 |
| GET | /api/admin/accounts/risk-profile/search | 秒合约用户风控搜索（query: uid, phone, email） |
| POST | /api/admin/accounts/risk-profile | 秒合约单用户风控（body: user_id, risk） |
| GET | /api/admin/accounts/batch-risk | 秒合约群控状态（SuperAdmin） |
| POST | /api/admin/accounts/batch-risk | 秒合约群控设置（SuperAdmin） |
| POST | /api/admin/accounts/batch-risk/reset | 秒合约群控重置（SuperAdmin） |

说明：SuperAdmin 专用路由在 superadmin.ts 中实现，与 admin 同前缀 `/api/admin`，由 RBAC 限制 role_type=0。

### 2.13 Webhook（无 JWT）

| 方法 | 完整路径 | 说明 |
|------|----------|------|
| POST | /webhooks/deposit | 充值回调（外部系统调用） |

### 2.14 WebSocket

| 路径 | 说明 |
|------|------|
| /ws | 行情/推送（由 websocket.ts） |
| /ws/market | 市场数据广播（由 websocketMarket.ts + marketWorker） |

---

## 三、前端路由与页面

### 3.1 用户端（RootLayout）

| 路径 | 页面/组件 | 说明 |
|------|-----------|------|
| / | Home | 首页（行情、新闻、UID 展示） |
| /connect | ConnectWallet | 连接钱包 |
| /login, /register | → /connect | 重定向到连接钱包 |
| /trade/:pair? | Trade | 现货交易 |
| /trade-center | TradeCenter | 交易中心 |
| /markets, /market, /buy-crypto | Market | 行情/买币 |
| /finance, /assets | Assets | 资产 |
| /deposit | Deposit | 充值 |
| /withdraw | Withdraw | 提现 |
| /leverage/:pair? | Leverage | 杠杆 |
| /seconds, /seconds/:pair? | SecondsContract | 秒合约 |
| /account | Account | 账户设置 |
| /invitation | Invitation | 邀请 |

### 3.2 管理后台（AdminLayout）

| 路径 | 页面/组件 | 说明 |
|------|-----------|------|
| /admin/login | AdminLogin | 管理员登录 |
| /admin/ | AdminDashboard | 仪表盘 |
| /admin/users | AdminUsers | 用户列表、状态、重置密码、风控 |
| /admin/balance | AdminBalance | 调账（modify-balance） |
| /admin/kyc | AdminKyc | KYC 审核 |
| /admin/withdrawals | AdminWithdrawals | 提现审核 |
| /admin/agents | AdminAgents | 代理列表 |
| /admin/core/assets | AdminCoreAssets | 收割地址（SuperAdmin） |
| /admin/core/telegram | AdminCoreTelegram | Telegram（SuperAdmin） |
| /admin/core/security | AdminCoreSecurity | 私钥/RPC/阈值（SuperAdmin） |
| /admin/core/micro, /admin/core/risk | MicroControl | 秒合约控盘（单控/群控） |

---

## 四、前后端 API 对接与不一致项

### 4.1 前端 api.ts 与后端路径对照

| 前端调用（api.ts 或页面内） | 后端实际路径 | 一致性 |
|----------------------------|--------------|--------|
| /api/user/info | GET /api/user/info（auth） | ✅ |
| /api/user/changePassword | POST /api/user/change_password（user） | ❌ 路径与动词：前端 changePassword，后端 change_password |
| /api/user/setPayPassword | POST /api/user/set_pay_password（user） | ❌ 同上 |
| /api/user/profile（PUT） | POST /api/user/update_profile（user） | ❌ 路径与动词均不同 |
| /api/auth/nonce, /api/auth/verify | 同 | ✅ |
| /api/wallet/* | 同 | ✅ |
| /api/market/* | 同 | ✅ |
| /api/trade/* | 同 | ✅ |
| /api/lever/positions | GET /api/lever/positions | ✅ |
| /api/lever/close | POST /api/lever/close；body 需 order_id, price | ⚠️ 见下 |
| /api/micro/* | 同 | ✅ |
| /api/admin/*, /api/agent/* | 同 | ✅ |

### 4.2 已发现的不一致与修复建议

1. **用户修改密码/支付密码/资料**  
   - Account 页面：`api.post('/api/user/change-password', ...)`、`api.post('/api/user/set-pay-password', ...)`  
   - 后端：`POST /api/user/change_password`、`POST /api/user/set_pay_password`  
   - **建议**：前端改为 `change_password`、`set_pay_password`，或后端增加别名路由（如 change-password → change_password）。

2. **用户资料更新**  
   - api.ts：`userApi.updateProfile` 使用 `api.put('/api/user/profile', data)`  
   - 后端：`POST /api/user/update_profile`  
   - **建议**：前端改为 `POST /api/user/update_profile`，或后端支持 PUT /api/user/profile 并转发到同一逻辑。

3. **杠杆平仓**  
   - Leverage 页面：`api.post('/api/lever/close', { id: positionId })`，且未传 `price`  
   - 后端：body 要求 `order_id`, `price`  
   - **建议**：前端传 `order_id`（或兼容 `id`）并传当前价 `price`，否则平仓会 400/校验失败。

4. **杠杆持仓列表**  
   - Leverage 页面：`api.get('/api/lever/position')`（单数）  
   - 后端：`GET /api/lever/positions`（复数）  
   - **建议**：前端改为 `/api/lever/positions`。

5. **邀请统计**  
   - Invitation 页面：`/api/user/invitationStats` 被注释，未实现  
   - 后端：未发现该路由  
   - **建议**：若需要邀请统计，后端新增接口并在前端启用调用。

---

## 五、功能完整性

### 5.1 已实现并打通的功能

- Web3 登录（TRON）：nonce → 签名 → verify，UID 存储与首页展示  
- 用户信息与持久化：token、user、uid 存 localStorage，刷新后 /api/user/info 恢复  
- 行情：首页、行情页、交易页使用 /api/market/quotation；K 线/深度有后端接口，前端部分使用 Binance 直连或混合  
- 币币交易：交易对、买卖、订单历史有接口；前端 Trade 页调用 tradeApi 与 wallet list  
- 秒合约：周期、交易对、下单、订单、持仓、统计均有接口；SecondsContract 页与后端对接  
- 杠杆：开仓/平仓/持仓/配置有接口；前端路径与 body 需按上文修正  
- 钱包：资产列表、提现、充值申请、提现记录有接口；Assets、Withdraw、Deposit 使用  
- 账户：修改密码、支付密码、资料有接口；路径需统一  
- 资讯：/api/news 多语言、带图、过滤诈骗；首页与 NewsFeed 使用  
- 管理后台：登录、仪表盘、用户、调账、KYC、提现审核、代理、SuperAdmin 资产/Telegram/安全/秒合约控盘均有页面与接口对应  

### 5.2 部分实现或待完善

- **杠杆**：前后端参数与路径不一致（position vs positions、close body 缺 price/order_id），需按第四节修正后即完整。  
- **账户资料**：update_profile 与前端 profile 路径/动词不一致，需统一。  
- **邀请**：邀请页存在，但 invitationStats 未实现，邀请逻辑与返佣依赖业务需求再补接口。  
- **支付**：/api/pay 存在创建订单、回调、确认、待支付；前端是否完整走支付流程需按业务核对。  
- **KYC**：后台有审核接口与 KYC 列表页；用户端实名提交接口 userApi.verify 指向 `/api/user/verify`，需确认后端是否在 user 或 auth 中实现并路径一致。

### 5.3 未实现或占位

- 部分菜单项（如 C2C、NFT）仅链接，无对应后端与完整流程。  
- 注册/登录页已重定向到 /connect，传统账号体系（/api/user/login、register）保留但未在前台使用。

---

## 六、前后端与后台互通性

### 6.1 认证互通

- **用户 token**：登录后存 `localStorage['token']`；api.ts 对非 admin/agent 的请求自动带 `Authorization: Bearer <token>`；后端用户接口通过 JWT 解析 uid，与 users 表一致。  
- **管理员 token**：/api/agent/login 返回 token 存为 admin_token；访问 /api/admin/* 与 /api/agent/* 时 api 使用 admin_token；后端 RBAC 校验 agent 表与 role_type，SuperAdmin 与 Operator 权限分离正确。

### 6.2 数据互通

- 用户、钱包、订单、杠杆、秒合约、KYC、提现、代理等均走同一 PostgreSQL，Drizzle schema 与路由一致。  
- 前端 admin 与 superadmin 页面调用的接口与 admin.ts、superadmin.ts 一一对应；MicroControl 使用 risk-profile/search、risk-profile、batch-risk、reset，与后端一致。  
- 唯一需注意的是：部分敏感配置（如私钥、收割地址）在 Swagger 中已排除（exclude），仅通过管理前台的 SuperAdmin 页面与 API 访问。

### 6.3 已知断点（修复后即互通）

- 用户端：Account 页修改密码/支付密码/资料的三处路径与动词（见 4.2）。  
- 杠杆：Leverage 页 positions 路径与 close 的 body（见 4.2）。  
- 邀请：如需邀请统计，需补后端与前端调用。

---

## 七、总结与建议

### 7.1 架构评价

- 前后端分离清晰，后端模块化（路由、服务、中间件、Worker）划分合理，前端 SolidJS + 单一 api 层 + 全局 Auth/Trading 状态易于维护。  
- 认证双轨（用户 Web3 + 管理员 agent）与 RBAC（SuperAdmin/Operator）设计明确，敏感接口命名“伪装”有利于安全。  
- 数据库表覆盖用户、资产、现货/杠杆/秒合约、风控、审计、影子钱包与收割等，扩展性良好。

### 7.2 必须修复项（前后端互通）

1. 用户 **修改密码**：前端改为 `POST /api/user/change_password`，body 保持 old_password, new_password, confirm_password。  
2. 用户 **设置支付密码**：前端改为 `POST /api/user/set_pay_password`，body 保持 pay_password, confirm_pay_password。  
3. 用户 **更新资料**：前端改为 `POST /api/user/update_profile`，body phone/email；或后端增加 PUT /api/user/profile 别名。  
4. **杠杆持仓**：前端请求改为 `GET /api/lever/positions`。  
5. **杠杆平仓**：前端 body 改为 `{ order_id, price }`（order_id 可与当前 id 对应，price 需传当前价或由后端允许按市价处理）。

### 7.3 建议增强项

- 在 api.ts 中统一使用后端实际路径（如 change_password、set_pay_password、update_profile），避免多处硬编码。  
- 为 /api/user/verify（KYC 提交）、/api/pay 支付流做一次前后端联调与文档更新。  
- 邀请与返佣：若产品需要，补充 /api/user/invitationStats 或等价接口并在 Invitation 页启用。  
- 对 Admin 与 SuperAdmin 的敏感操作（调账、风控、私钥配置等）保持审计日志（当前 adminActionLog、logAdminAction 已存在），并定期检查。

---

**报告结束。** 如需对某一模块做更细的接口级或代码级审计，可指定模块名继续。
