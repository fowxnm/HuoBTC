# BTC Exchange - Windows 本地部署说明

项目已移除 Docker，改为本机直接运行，便于排查问题。

## 一、环境准备

1. **PostgreSQL**（端口 5432）  
   - 安装：https://www.postgresql.org/download/windows/  
   - 创建数据库：`createdb btc_exchange` 或用 pgAdmin 建库 `btc_exchange`

2. **Redis**（端口 6379）  
   - Windows 版：https://github.com/microsoftarchive/redis/releases  
   - 或 WSL 内安装：`sudo apt install redis-server && redis-server`

3. **Bun**（运行后端与前端）  
   - 安装：https://bun.sh  
   - 命令行执行：`bun --version` 确认安装成功

## 二、配置

1. 复制后端环境配置：
   ```text
   复制 backend\.env.example 为 backend\.env
   ```

2. 编辑 `backend\.env`，确保本机连接串正确：
   ```env
   DATABASE_URL=postgres://postgres:你的密码@localhost:5432/btc_exchange
   REDIS_URL=redis://localhost:6379
   PORT=8000
   ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
   ```

3. 首次运行可同步数据库表结构（在项目根目录）：
   ```bat
   cd backend
   bunx drizzle-kit push
   ```
   （如无 drizzle 可先执行 `bun install`）

## 三、启动

**方式一：一键启动（推荐）**  
双击运行根目录下的 **`start-windows.bat`**，会打开两个命令行窗口：  
- 一个运行后端（端口 8000）  
- 一个运行前端（端口 3000）  

**方式二：手动启动**  
- 终端 1（后端）：`cd backend` → `bun run dev`  
- 终端 2（前端）：`cd frontend` → `bun run dev`  

然后在浏览器打开：**http://localhost:3000**

## 四、端口与代理

- 前端开发服务器：**3000**（Vite 会把 `/api` 代理到后端 8000）  
- 后端 API：**8000**  
- PostgreSQL：**5432**  
- Redis：**6379**  

确保以上端口未被占用。若 5432 / 6379 未启动，后端会报连接失败，便于直接在本机日志里排查。
