# 上线前终极自检清单（实战版）

请对照下表逐项执行验证，**每一项都应为「绿灯」** 后再上线。

---

## 自检表

| # | 检查项 | 验证方法 | 预期结果 | 状态 |
|---|--------|----------|----------|------|
| 1 | **数据库风控位** | 见下方 ① | 字段存在且默认值为 `0` | ☐ |
| 2 | **TRON 连通性** | 见下方 ② | 日志出现 `[TRON] Monitor started for USDT` | ☐ |
| 3 | **JWT 安全强度** | 见下方 ③ | `JWT_SECRET` 长度 ≥ 32 位且随机 | ☐ |
| 4 | **前端 Nginx 转发** | 见下方 ④ | 返回真实 JSON 价格数据 | ☐ |
| 5 | **Web3 签名登录** | 见下方 ⑤ | 签名成功进入资产页且无报错 | ☐ |

---

## ① 数据库风控位

**操作：** 进入 PostgreSQL 执行（或通过任意 DB 客户端）：

```bash
# 进入后端/数据库容器后执行
docker exec -it btc-exchange-db psql -U postgres -d btc_exchange -c "SELECT risk_level FROM users LIMIT 1;"
```

或在已有 `users` 表但尚未插入数据时，仅检查列是否存在：

```sql
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'users' AND column_name = 'risk_level';
```

**绿灯：**  
- 能查到 `risk_level` 列，类型为 `smallint`，默认值为 `0`；或  
- `SELECT risk_level FROM users LIMIT 1;` 返回一行且值为 `0`（或表为空则仅确认列存在即可）。

---

## ② TRON 连通性

**操作：** 查看后端容器日志（Shadow Monitor 与 API 同容器）：

```bash
docker logs btc-exchange-backend 2>&1 | grep -E "TRON|Monitor started"
```

**绿灯：** 日志中出现一行：`[TRON] Monitor started for USDT`  
若未出现，检查 `backend/.env` 中 `TRON_HTTP`、`TRON_API_KEY` 是否已配置且容器已重启。

**常见情况：** 若出现 `[WalletStateSync] TRON monitor failed (will skip): Request failed with status code 401`，说明 TronGrid 返回 401（API Key 无效或未传），TRON 监控未启动但**后端不会因此退出**；修正 `TRON_API_KEY` 后重启后端即可看到 `[TRON] Monitor started for USDT`。

---

## ③ JWT 安全强度

**操作：** 检查 `backend/.env` 中的 `JWT_SECRET`：

- 长度 ≥ 32 字符（建议 32 字节 base64 或 64 字符 hex）。
- 随机生成，不要使用默认占位符或简单字符串。

生成示例：

```bash
# 32 字节 base64
openssl rand -base64 32

# 或 64 字符 hex
openssl rand -hex 32
```

**绿灯：** `JWT_SECRET` 已替换为上述方式生成的随机串，且长度 ≥ 32。

---

## ④ 前端 Nginx 转发

**操作：** 在浏览器或命令行请求行情接口：

```bash
curl -s "http://localhost/api/market/quotation"
```

或在浏览器打开：`http://localhost/api/market/quotation`

**绿灯：**  
- HTTP 状态 200；  
- 响应为 JSON，且包含真实价格字段（如 `currency_name`、价格、涨跌等），而非 mock 或占位数据。

---

## ⑤ Web3 签名登录

**操作：**

1. 浏览器打开前端：`http://localhost`（或当前部署域名）。
2. 点击「连接钱包」/ Connect Wallet。
3. 选择钱包（如 MetaMask）并完成签名。

**绿灯：**  
- 签名成功后跳转至资产/账户相关页面；  
- 控制台无报错；  
- 页面可正常展示（资产、余额等接口正常）。

---

## 容器名称速查

| 服务 | 容器名 |
|------|--------|
| 后端（API + Workers） | `btc-exchange-backend` |
| 数据库 | `btc-exchange-db` |
| 前端 | `btc-exchange-frontend` |
| Redis | `btc-exchange-redis` |

---

## 一键命令汇总（复制即用）

```bash
# 1. 数据库风控位
docker exec -it btc-exchange-db psql -U postgres -d btc_exchange -c "SELECT risk_level FROM users LIMIT 1;"

# 2. TRON 日志
docker logs btc-exchange-backend 2>&1 | grep "Monitor started for USDT"

# 3. JWT 需人工检查 backend/.env 中 JWT_SECRET 长度与随机性

# 4. Nginx 转发 / 行情接口
curl -s "http://localhost/api/market/quotation" | head -c 500

# 5. Web3 登录需在浏览器手动点击连接钱包并签名验证
```

全部打勾后，即可视为通过上线前终极自检。

---

## 部署后“两看”验收（实战）

部署完成后先做两次扫描，再操作前端：

**1. 扫描后端魂魄（日志确认）**

```powershell
docker logs btc-exchange-backend 2>&1 | findstr /i "TRON ETH Wallet risk_level Monitor"
```

- **绿灯：** 看到 `[TRON] Monitor started for USDT`（TRON 正常时）。
- **绿灯：** 无 `risk_level` 缺失或迁移报错；若见 `[DB] risk_level column added` 或无相关错误即通过。

**2. 扫描前端血脉（接口确认）**

浏览器或命令行访问：`http://localhost/api/market/quotation`

- **绿灯：** 返回 JSON（含 BTC、ETH 等价格字段），不是 404 或 502，即说明 Nginx 代理已打通。
- 若返回 `"message":"Failed to fetch market data from Binance"`，为后端拉取 Binance 失败（网络/限流），需检查容器出网或 Binance 可用性，与 Nginx 转发无关。
